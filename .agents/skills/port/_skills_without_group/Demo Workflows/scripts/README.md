# Scripts (demo)

Per [Agent Skills](https://agentskills.io/specification), `scripts/` may contain executable helpers.

This demo stores documentation only. Production skills might include:

- `validate.py` — preflight checks
- `export.sh` — CI integration

Agents should read script contents before suggesting execution.
