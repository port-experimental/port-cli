# Session Context

## User Prompts

### Prompt 1

Update the example ado blueprint sync we should have: a new blueprint blueprint (so we can select the blueprints easily) and the environment blueprint and entities should not be needed and instead we will have an ENUM in the self service that shows Development, Production. Also update the ADO pipeline credentials to be PORT_CLIENT_ID and PORT_CLIENT_SECRET for the production environment and PORT_DEV_CLIENT_ID and PORT_DEV_CLIENT_SECRET for the dev environment. This is an implementation only for ...

